from django.db import models
# TO DO: Finish Blog Models and uncomment them
"""
class Post(models.Model):
    title = CharField(max_length=155, editable=True)
    intro = TextField(null=True, editable=True)
    body = TextField(null=True, editable=True)
    conclusion = TextField(null=True, editable=True)
    genre =
    author = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=CASCADE)
    created =
    modified =
    sub_blog = models.
    votes = models.IntegerField(
"""